# Intermediate step between config and CSS. Regenerated by `task tokens`.
design-tokens/

# The built theme is committed (see README), but the type files are TypeScript only.
design-tokens-build/*.d.ts

# Fetched by `task vendor`.
vendor/

# go-task checksum cache.
.task/

# Editor swap files.
.*.sw[a-p]
